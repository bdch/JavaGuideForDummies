package Solutions.Levels;

public class Level_2 {

    // To define a main method, you can use the following syntax:

    public static void main(String[] args) {
        // First, you create a variable called 'radius' and assign a value of 5 to it.
        int radius = 5;
        // Second, you create a variable called 'area' and assign it.
        double area = Math.PI * radius * radius;
        // Lastly, you need to calculate the area of a circle using the formula: area = Math.PI * radius * radius and print the result.
        System.out.println(area);
    }
}
